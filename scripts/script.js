const game = (() => {
    'use strict';
    const gameBoard = (() => {
        let gameArray = [
            0, 0, 0,
            0, 0, 0,
            0, 0, 0,
        ];

        const renderGameBoard = () => {
            
        }
        return {
            gameArray
        };

    })();
    return {
        gameBoard
    };

})();